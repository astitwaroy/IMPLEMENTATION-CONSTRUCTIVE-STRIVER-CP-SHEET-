#include <iostream>
#include <algorithm>
using namespace std;

int solve() {
    int a, b, c;
    cin >> a >> b >> c;
    int maxV = max({a, b, c});
    int minV = min({a, b, c});
    int res = maxV - minV;
    return res;
}

int main() {
    cout << solve() << std::endl;
    return 0;
}
 
